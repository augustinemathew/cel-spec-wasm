load("@rules_cc//cc:defs.bzl", "cc_binary")

# Throwaway empirical probe for two M7B-polish corner cases.  Run
# via `bazel run -c opt //compiler_v2/throwaway:cel_cpp_corner_probe`.
# Delete once both items are verified.  See the source-file header
# for the four expressions it evaluates.
cc_binary(
    name = "cel_cpp_corner_probe",
    srcs = ["cel_cpp_corner_probe.cc"],
    copts = ["-std=c++17"],
    tags = ["manual"],
    testonly = True,
    deps = [
        "@cel-cpp//common:ast",
        "@cel-cpp//common:ast_proto",
        "@cel-cpp//common:value",
        "@cel-cpp//compiler",
        "@cel-cpp//compiler:compiler_factory",
        "@cel-cpp//compiler:standard_library",
        "@cel-cpp//internal:testing_descriptor_pool",
        "@cel-cpp//runtime",
        "@cel-cpp//runtime:activation",
        "@cel-cpp//runtime:runtime_builder",
        "@cel-cpp//runtime:runtime_options",
        "@cel-cpp//runtime:standard_runtime_builder_factory",
        "@com_google_absl//absl/log:absl_check",
        "@com_google_absl//absl/status",
        "@com_google_absl//absl/status:statusor",
        "@com_google_absl//absl/strings:string_view",
        "//proto/cel/expr:checked_cc_proto",
        "@com_google_protobuf//:protobuf",
    ],
)
