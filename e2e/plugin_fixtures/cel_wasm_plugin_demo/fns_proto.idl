// celfn IDL fixture exercising the proto path through the
// cel_wasm_plugin macro (m24 §8: proto crosses the
// Component-Model boundary as serialized bytes).
//
// Pairs with user_fns_proto.cc + user.proto and the
// `demo_plugin_proto` target (BUILD.bazel).

Module customfn;

bool             @plugin.is_adult(proto(acme.User) u);
proto(acme.User) @plugin.capitalize(proto(acme.User) u);
