load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")

package(default_visibility = ["//:internal"])

# Top-level pipeline facade.  Stitches parse → check → resolve → layout →
# module-setup → lower so the CLI / host_loader / e2e tests don't each
# re-wire the passes.  Output is a `CompiledArtifact` bundling the typed
# AST, the static layout, the Binaryen module, the `$eval` handle, and
# (optionally) the serialised wasm bytes.
cc_library(
    name = "compile",
    srcs = ["compile.cc"],
    hdrs = ["compile.h"],
    deps = [
        "//abi:cel_abi_cc_proto",
        "//abi:cel_abi_emit",
        "//abi:runtime_catalogue",
        "//compiler/codegen:expr_lower",
        "//compiler/codegen:layout_pass",
        "//compiler/codegen:module",
        "//compiler/codegen:overload_table",
        "//compiler/codegen:resolve_pass",
        "//compiler/frontend:parse_and_check",
        "//compiler/ir:typed_ast",
        "@binaryen//:binaryen",
        "@com_google_absl//absl/base:core_headers",
        "@com_google_absl//absl/container:flat_hash_set",
        "@com_google_absl//absl/log:absl_check",
        "@com_google_absl//absl/status",
        "@com_google_absl//absl/status:statusor",
        "@com_google_absl//absl/strings",
        "@com_google_absl//absl/types:span",
    ],
)

cc_test(
    name = "compile_test",
    srcs = ["compile_test.cc"],
    deps = [
        ":compile",
        "//testdata:e2e_fixture_cc_proto",
        "@binaryen//:binaryen",
        "@com_google_absl//absl/status",
        "@com_google_absl//absl/status:status_matchers",
        "@com_google_googletest//:gtest_main",
        "@com_google_protobuf//:protobuf",
    ],
)
